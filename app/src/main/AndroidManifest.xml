<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AI_Travel_Planner"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AI_Travel_Planner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".StartScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".SigninScreen" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".DescribeScreen" />
        <activity android:name=".PlanScreen" />
        <activity android:name=".WeatherScreen" />
        <activity android:name=".CalendarScreen" />
        <activity android:name=".GuideScreen" />
        <activity android:name=".AddItineraryScreen" />
        <activity android:name=".FriendScreen" />



    </application>
</manifest>
